doctype html
html(lang=i18n.locale)
    head
        title Test
        script(type='text/javascript', src=i18n._jsSrc)
    body

h1 #{i18n.__('hello')}
div Current locale
p #{i18n._locale}

p Locales
    ul
        each locale in i18n._locales
            li: a(href=locale.path)= locale.name